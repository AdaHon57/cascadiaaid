# Copy this file to .env.local. Keep secrets out of source control.

# Required for recovery question answering (lib/rag): answers and embeddings.
OPENAI_API_KEY=

# Optional RAG settings (see docs/rag.md).
# RAG_ANSWER_MODEL=gpt-4o-mini
# RAG_ANSWER_TEMPERATURE=0
# RAG_EMBEDDING_PROVIDER=auto
# RAG_EMBEDDING_MODEL=text-embedding-3-small
# RAG_EMBEDDING_DIMENSIONS=512
# RAG_TOP_K=6
# RAG_MAX_CHUNKS_PER_SOURCE=3
